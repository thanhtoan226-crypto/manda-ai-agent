[project]
name = "manda-ai-agent"
version = "0.1.0"
description = "Manda AI Agent - Full-stack AI agent application"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
